const router = require("express").Router();
const userRoutes = require("./videoRoutes");
const thoughtRoutes = require("./userRoutes");

// router.use("/videos", videoRoutes);
// router.use("/users", userRoutes);

module.exports = router;
